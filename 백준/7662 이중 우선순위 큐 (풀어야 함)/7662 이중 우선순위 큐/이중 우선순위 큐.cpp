#include <iostream>
using namespace std;

// 힙을 하고 오는게 좋을듯
// 삽입은 상관없으나 삭제의 경우 연산량이 너무 많을 수 있음

// 최대 힙, 최소 힙으로 나눠서 삽입의 경우 어디에 넣을지 정하면 됨
// 삭제의 경우 -1이면 최소 힙, 1이면 최대 힙에서 지우고 재배치하면 됨

int main(void)
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);

	int T;
	cin >> T;

	for (int i = 0; i < T; i++) {

	}


}